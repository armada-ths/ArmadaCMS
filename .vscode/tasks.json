{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start local Postgres",
      "type": "shell",
      "command": "docker",
      "args": ["compose", "-f", "docker-compose.db.yml", "up", "-d"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
