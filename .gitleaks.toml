title = "ArmadaCMS Gitleaks configuration"

[extend]
useDefault = true

[[allowlists]]
description = "Terraform maps DB_PASSWORD to the identically named Secret Manager resource"
regexTarget = "line"
regexes = [
  '''^\s*DB_PASSWORD\s*=\s*"DB_PASSWORD"\s*$''',
]